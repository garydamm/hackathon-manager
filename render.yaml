# Render Blueprint configuration for hackathon-manager
# See: https://render.com/docs/blueprint-spec

databases:
  - name: hackathon-db
    plan: free
    databaseName: hackathon_manager
    user: hackathon_user

services:
  # Backend API - Spring Boot application
  - type: web
    name: hackathon-api
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: hackathon-db
          property: connectionString
      - key: DB_USERNAME
        fromDatabase:
          name: hackathon-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: hackathon-db
          property: password
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        sync: false
      - key: SPRING_PROFILES_ACTIVE
        value: prod

  # Frontend - React/Vite static site
  - type: web
    name: hackathon-app
    runtime: static
    plan: free
    buildCommand: cd frontend && npm ci && npm run build
    staticPublishPath: frontend/dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: hackathon-api
          envVarKey: RENDER_EXTERNAL_URL
