# Authentication Token Storage Configuration
# Set to 'true' to use HttpOnly cookies for token storage (recommended for production)
# Set to 'false' to use localStorage for token storage (legacy mode)
# Default: false (localStorage)
VITE_USE_COOKIES=false
